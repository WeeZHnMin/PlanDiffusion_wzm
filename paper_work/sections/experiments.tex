\section{实验}
\label{sec:experiments}

\subsection{实验设置}
\label{subsec:setup}

\paragraph{评估指标}
我们从图拓扑合法性、坐标回归精度和节点类型预测三个维度评估各子模块，
并以图结构分布指标衡量端到端生成质量。
所有指标均在 10,000 条独立测试集上计算。

\begin{itemize}
    \item \textbf{图有效率（Graph Validity Rate, GVR）}：
    生成图中连通且无孤立节点的样本占比，反映图拓扑生成的合法性。

    \item \textbf{度分布 KL 散度（Degree KL）}：
    生成图与真实图的节点度分布之间的 KL 散度，
    衡量端到端生成的图结构分布质量。

    \item \textbf{节点数分布 KL 散度（Node-count KL）}：
    生成图与真实图的节点数分布之间的 KL 散度，
    衡量模型对平面图规模的建模能力。

    \item \textbf{坐标 RMSE（Coord-RMSE）}：
    给定真实图结构条件下，去噪预测坐标与真实坐标的均方根误差，
    衡量节点坐标扩散子模块的回归精度。

    \item \textbf{类型准确率（Type Acc.）}：
    给定真实坐标和图结构条件下，节点类型预测与真实类型的匹配率，
    衡量类型预测子模块的分类精度。
\end{itemize}

\paragraph{相关方法对比}
由于现有方法在输入模态、数据集和图表示方式上与本文存在根本差异，
难以在统一评估协议下进行定量对比。
表~\ref{tab:method_comparison} 从方法特性角度对比各代表性工作。

\begin{table}[h]
\centering
\caption{方法特性对比}
\label{tab:method_comparison}
\small
\begin{tabular}{lcccc}
\toprule
方法 & 输入模态 & 显式图结构 & 端到端文本驱动 & 拓扑约束 \\
\midrule
Architext~\cite{galanos2023architext}             & 文本   & $\times$ & $\checkmark$ & $\times$ \\
HouseDiffusion~\cite{shabani2023housediffusion}   & 气泡图 & $\checkmark$ & $\times$ & 部分 \\
ChatHouseDiffusion~\cite{qin2025chathousediffusion} & 文本 & $\times$ & $\checkmark$ & $\times$ \\
\midrule
PlanDiffusion（本文）                             & 文本   & $\checkmark$ & $\checkmark$ & $\checkmark$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{实现细节}
三个子模块均在单张 NVIDIA GPU 上独立训练。
图拓扑生成（LlamaForCausalLM，约 20M 参数）在 RTX 3060（6 GB，笔记本）上训练，
batch size 22，学习率余弦退火 $10^{-4} \to 10^{-5}$，
共约 345 万步（含中途停止后续训），累计训练时长约 211 小时。
节点坐标扩散（NodeDiffusionTransformer，约 15M 参数）在 RTX 3080（10 GB）上训练，
batch size 384，训练 250k steps。
节点类型预测（NodeTypeClassifier，约 15M 参数）在 RTX 3060（6 GB，笔记本）上训练，
batch size 64，共训练 320k steps，累计训练时长约 45 小时。
推理时三阶段串联执行：图拓扑生成（自回归解码）$\to$
坐标扩散（DDPM 1000步逆采样）$\to$ 类型分类（单次前向）$\to$ render.py 渲染输出。
\todo{补充：端到端推理耗时。}

\subsection{主实验结果}
\label{subsec:main_results}

表~\ref{tab:main_results} 报告了 PlanDiffusion 各子模块的定量评估结果，
测试集共 10,000 条样本。
由于现有方法与本文在输入模态、数据集及图表示方式上均不一致，
此处仅报告本文方法的完整指标。

\begin{table}[h]
\centering
\caption{PlanDiffusion 各子模块定量结果}
\label{tab:main_results}
\small
\begin{tabular}{lcc}
\toprule
子模块 & 指标 & 结果 \\
\midrule
\multirow{2}{*}{图拓扑生成（$\theta_1$）}
    & Degree KL↓      & 0.0108 \\
    & Node-count KL↓  & 0.0051 \\
\midrule
节点坐标扩散（$\theta_2$） & Coord-RMSE↓ & \todo{} \\
\midrule
\multirow{2}{*}{节点类型预测（$\theta_3$）}
    & Node Type Acc.↑ & 98.71\% \\
    & Face Type Acc.↑ & 99.06\% \\
\bottomrule
\end{tabular}
\end{table}

\todo{填入实验数值后删除本行。}

\subsection{消融实验}
\label{subsec:ablation}

\subsubsection{推理约束消融（图拓扑生成）}

表~\ref{tab:ablation_constraints} 在相同模型权重下，
逐步加入五个解码约束，评估图结构合法性指标的变化。
\textbf{图有效率（GVR）}：生成图连通且无孤立节点的样本占比；
\textbf{无三角率}：生成图中不含三节点闭环的比例；
\textbf{最小度合格率}：所有节点度数 $\geq 2$ 的样本占比。

\begin{table}[h]
\centering
\caption{推理约束消融：逐步启用解码约束对图合法性的影响}
\label{tab:ablation_constraints}
\small
\begin{tabular}{lcccccc}
\toprule
约束配置 & C1 & C2 & C3 & C4 & C5 & GVR↑ \\
\midrule
无约束                        & $\times$ & $\times$ & $\times$ & $\times$ & $\times$ & 99.6\% \\
+C1（因果树 $p_k < k$）       & $\checkmark$ & $\times$ & $\times$ & $\times$ & $\times$ & 99.9\% \\
+C2（SEP 时机控制）           & $\checkmark$ & $\checkmark$ & $\times$ & $\times$ & $\times$ & 100.0\% \\
+C3（禁三节点环）             & $\checkmark$ & $\checkmark$ & $\checkmark$ & $\times$ & $\times$ & 100.0\% \\
+C4（最小度 $\geq 2$）        & $\checkmark$ & $\checkmark$ & $\checkmark$ & $\checkmark$ & $\times$ & 100.0\% \\
+C5（$N \geq 9$）             & $\checkmark$ & $\checkmark$ & $\checkmark$ & $\checkmark$ & $\checkmark$ & 100.0\% \\
\bottomrule
\end{tabular}
\end{table}


\subsubsection{注意力结构消融（节点坐标扩散）}

为验证双流注意力设计的有效性，
我们在相同数据集和相同训练步数下分别从头训练三个结构变体，
并对比坐标回归精度（表~\ref{tab:ablation_attn}）。
三个变体除注意力结构外其余超参数完全一致。

训练数据为原始 6k 平面图数据集经 8 倍坐标增强后所得约 48k 样本，
三个变体均以 batch size 384 各独立训练 250k steps；
评估采用从同一 6k 数据集中随机抽取的 1000 条样本，
以 Coord-RMSE 衡量 DDPM 1000 步逆采样后的坐标重建精度。
由于消融三变体均使用相同数据来源训练与评估，
各变体之间的相对比较仍可有效反映注意力结构本身的影响。

\begin{table}[h]
\centering
\caption{注意力结构消融：各变体独立训练后的坐标扩散精度对比}
\label{tab:ablation_attn}
\small
\begin{tabular}{lcc}
\toprule
注意力变体 & 描述 & Coord-RMSE↓ \\
\midrule
仅邻接局部注意力（AdjAttn only）  & 每节点仅关注直连邻居           & \todo{} \\
仅全局注意力（GlobalAttn only）   & 节点间无掩码全局自注意力       & \todo{} \\
双流（AdjAttn + GlobalAttn）      & 局部 + 全局并行，本文完整模型  & \todo{} \\
\bottomrule
\end{tabular}
\end{table}

\todo{填入注意力消融数值后删除本行。}

\subsection{定性结果}
\label{subsec:qualitative}

\begin{figure}[htbp]
    \centering
    \todo{插入端到端生成结果图：每行为一个文本条件，
    展示生成的顶点图（含边和节点类型着色）以及 render.py 输出的彩色平面图。}
    \caption{
        PlanDiffusion 端到端生成结果。
        每组从左至右依次为：输入文本描述、生成顶点图（节点按组合类型着色）、
        渲染平面图（房间按类型填色，标注房间名）。
    }
    \label{fig:qualitative}
\end{figure}

\todo{填入定性结果图后删除本行。}
