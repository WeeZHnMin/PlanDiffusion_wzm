# Always compile from the directory containing the root .tex file.
# This keeps relative paths such as figures/foo.pdf and sections/bar.tex stable
# when LaTeX Workshop invokes latexmk from the workspace root.
$do_cd = 1;
